import {ObjectDataType} from "../dummies/dummyInterface";

export const getRectPositions = (target: ObjectDataType, canvasSize: [number, number]) => {
  //todo
  return [10, 20, 80, 20, 10, 30, 80, 20, 80, 30]
}